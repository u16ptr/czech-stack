---
import { getEntry, render } from 'astro:content';

import BaseLayout from '@layouts/BaseLayout.astro';

const post = await getEntry('js', 'post');
const { Content } = await render(post);
---

<BaseLayout title="Home">
  <Content />
</BaseLayout>
